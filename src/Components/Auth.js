function Auth(props) {
  // console.log("action : ", props.history.action);
  // console.log("props in auth are : ", JSON.stringify(props));
    // if(props.history.action === "POP") {
    //     props.history.push("/")
    // } else {

    // }

  return(
      <div>
          This is Auth 
      </div>
  )
}

export default Auth;